"use strict"


// create array
const array =[1,2,3];
console.log(array);
console.log(typeof(array));   // output is object

// create object
const arrayobj ={
    0:1,
    1:2,
    2:3,
}
console.log(arrayobj);
console.log(typeof(arrayobj));   // output is object



//how define array is array
console.log(Array.isArray(array));  // output is  true   .  true means this is array 